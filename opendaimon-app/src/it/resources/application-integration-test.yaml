# Full settings for @ActiveProfiles("integration-test") (single profile; do not combine with "test").

spring:
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  autoconfigure:
    exclude:
      - org.springframework.ai.model.chat.memory.autoconfigure.ChatMemoryAutoConfiguration
      - org.springframework.ai.model.chat.memory.repository.jdbc.autoconfigure.JdbcChatMemoryRepositoryAutoConfiguration

open-daimon:
  rest:
    enabled: false
  ui:
    enabled: false
  telegram:
    enabled: true
    token: test-token
    username: test-bot
    start-message: Hello from integration test
    file-upload:
      enabled: false
  common:
    assistant-role: You are a helpful assistant
    max-output-tokens: 1000
    max-reasoning-tokens: 500
    max-user-message-tokens: 4000
    max-total-prompt-tokens: 32000
    summarization:
      message-window-size: 5
      max-window-tokens: 16000
      max-output-tokens: 2000
      prompt: "You are an assistant. Create a summary in JSON. Conversation:"
    chat-routing:
      ADMIN:
        max-price: 0.5
        required-capabilities:
          - AUTO
        optional-capabilities: []
      VIP:
        max-price: 0.5
        required-capabilities:
          - CHAT
        optional-capabilities:
          - TOOL_CALLING
          - WEB
      REGULAR:
        max-price: 0.0
        required-capabilities:
          - CHAT
        optional-capabilities: []
  ai:
    openrouter:
      enabled: false
    deepseek:
      enabled: false
    spring-ai:
      enabled: true
      openrouter-auto-rotation:
        models:
          enabled: true
          api:
            key: ${OPENROUTER_KEY:}
            url: https://openrouter.ai/api
          refresh-initial-delay: 5s
          refresh-interval: 1h
          filters:
            include-model-ids:
              - meta-llama/llama-3.3-70b-instruct:free
              - mistralai/mistral-small-3.1-24b-instruct:free
          ranking:
            enabled: true
            retry-max-attempts: 3
            latency-ewma-alpha: 0.2
            cooldown429: 10m
            cooldown5xx: 5m
      history-window-size: 10
      timeouts:
        response-timeout-seconds: 120
        stream-timeout-seconds: 120
      serper:
        api:
          key: test-key
      rag:
        enabled: false
      models:
        list:
          - name: openrouter/auto
            capabilities:
              - AUTO
              - CHAT
              - VISION
            provider-type: OPENAI
            priority: 1
